// next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['i.pinimg.com']  // Add more domains as needed
    },
  };
  
  export default nextConfig;
  